---
phase: 01-theme-system-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyhwpxlib/themes.py
  - tests/test_themes.py
autonomous: true
requirements: [TS-1]

must_haves:
  truths:
    - "Theme dataclass hierarchy (Theme, Palette, FontSet, SizeSet, Margins) exists and is frozen"
    - "10 built-in themes are defined with fully expanded color palettes (not just 4 base colors)"
    - "Default theme palette exactly matches current DS dict values"
    - "_make_table_presets(palette) derives TABLE_PRESETS from any palette"
    - "Test file exists with failing tests for all TS-1 sub-requirements"
  artifacts:
    - path: "pyhwpxlib/themes.py"
      provides: "Theme dataclass tree + BUILTIN_THEMES dict + _make_table_presets()"
      exports: ["Theme", "Palette", "FontSet", "SizeSet", "Margins", "BUILTIN_THEMES", "_make_table_presets"]
    - path: "tests/test_themes.py"
      provides: "Test suite for TS-1 and TS-2 requirements"
      min_lines: 80
  key_links:
    - from: "pyhwpxlib/themes.py"
      to: "pyhwpxlib/builder.py"
      via: "BUILTIN_THEMES['default'].palette matches DS dict"
      pattern: "primary.*395da2"
---

<objective>
Create the Theme data model (dataclasses) and 10 built-in themes, plus the test scaffold for the entire phase.

Purpose: Establish the data layer that the builder integration (Plan 03) will wire in. The default theme must produce identical color values to the current DS dict for backward compatibility.

Output: `pyhwpxlib/themes.py` with complete theme definitions, `tests/test_themes.py` with tests for TS-1 and TS-2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-theme-system-core/01-RESEARCH.md

<interfaces>
<!-- Current DS dict values (pyhwpxlib/builder.py:44-57) — default theme MUST match these exactly -->
DS = {
    'primary': '#395da2',
    'primary_dim': '#2b5195',
    'on_primary': '#f7f7ff',
    'on_surface': '#2b3437',
    'on_surface_var': '#586064',
    'surface': '#f8f9fa',
    'surface_low': '#f1f4f6',
    'surface_high': '#e3e9ec',
    'primary_container': '#d8e2ff',
    'outline_var': '#abb3b7',
    'error': '#9f403d',
    'tertiary_container': '#e2dbfd',
}

<!-- TABLE_PRESETS structure (pyhwpxlib/builder.py:59-100) — _make_table_presets must produce this shape -->
TABLE_PRESETS = {
    'corporate': {'header_bg': str, 'header_text': str, 'cell_margin': tuple, 'header_height': int, 'row_height': int, 'header_align': str, 'data_align': str, 'stripe_color': str},
    'government': { ... },
    'academic': { ... },  # academic has empty strings for colors (no background)
    'default': { ... },
}

<!-- 10 palette bases from design_guide.md -->
PALETTE_BASES = {
    'administrative_slate': ('#395da2', '#cbe7f5', '#9f403d', '#f8f9fa'),
    'forest':               ('#2C5F2D', '#97BC62', '#F5F5F5', '#F9FBF7'),
    'warm_executive':       ('#B85042', '#E7E8D1', '#A7BEAE', '#FBF9F7'),
    'ocean_analytics':      ('#065A82', '#1C7293', '#21295C', '#F5F9FB'),
    'coral_energy':         ('#F96167', '#F9E795', '#2F3C7E', '#FFFDF7'),
    'charcoal_minimal':     ('#36454F', '#F2F2F2', '#212121', '#FFFFFF'),
    'teal_trust':           ('#028090', '#00A896', '#02C39A', '#F5FBFA'),
    'berry_cream':          ('#6D2E46', '#A26769', '#ECE2D0', '#FDF9F5'),
    'sage_calm':            ('#84B59F', '#69A297', '#50808E', '#F7FAF9'),
    'cherry_bold':          ('#990011', '#FCF6F5', '#2F3C7E', '#FFFCFC'),
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Theme dataclass hierarchy and 10 built-in themes</name>
  <files>pyhwpxlib/themes.py, tests/test_themes.py</files>
  <behavior>
    - test_default_palette_matches_ds: BUILTIN_THEMES['default'].palette.primary == '#395da2', .on_primary == '#f7f7ff', etc. (all 12 DS dict values)
    - test_all_themes_exist: BUILTIN_THEMES has exactly 10 entries with keys: default, forest, warm_executive, ocean_analytics, coral_energy, charcoal_minimal, teal_trust, berry_cream, sage_calm, cherry_bold
    - test_theme_is_frozen: Assigning theme.palette = X raises FrozenInstanceError
    - test_palette_no_none_values: Every palette field in every theme is a non-empty string starting with '#'
    - test_default_font_set: FontSet() defaults all fields to '함초롬돋움'
    - test_default_size_set: SizeSet() has h1=24, h2=18, h3=16, h4=14, body=11, caption=10
    - test_make_table_presets_corporate: _make_table_presets(default_palette)['corporate']['header_bg'] == '#395da2'
    - test_make_table_presets_academic: _make_table_presets(any_palette)['academic']['header_bg'] == '' (academic never has colors)
    - test_make_table_presets_shape: Result has keys corporate/government/academic/default, each with header_bg/header_text/cell_margin/header_height/row_height/header_align/data_align/stripe_color
  </behavior>
  <action>
    1. Create tests/test_themes.py FIRST with all tests from the behavior block above. Run tests — they must all FAIL (RED).

    2. Create pyhwpxlib/themes.py:
       - Define frozen dataclasses: Palette, FontSet, SizeSet, Margins, Theme (exactly as shown in RESEARCH Pattern 1)
       - Palette fields: primary, on_primary, secondary, accent, surface, on_surface, on_surface_var, surface_low, surface_high, primary_container, outline_var, error, tertiary_container, primary_dim
         (14 fields total — matches DS dict + secondary/accent from design guide)
       - Define `_make_table_presets(palette: Palette) -> dict` that produces the same TABLE_PRESETS shape, deriving header_bg/header_text/stripe_color from palette. Academic preset always uses empty strings.
       - Define BUILTIN_THEMES dict with 10 themes. The 'default' theme is 'administrative_slate' with palette values EXACTLY matching the current DS dict. Name it 'default' (not 'administrative_slate') in the dict.
       - For each of the other 9 themes: expand the 4 base colors (Primary, Secondary, Accent, Surface) into full Palette with on_primary, on_surface, on_surface_var, surface_low, surface_high, primary_container, outline_var, error, tertiary_container, primary_dim derived manually:
         - on_primary: '#f7f7ff' for dark primaries (value < 128), '#2b3437' for light primaries
         - on_surface: '#2b3437' (dark text, consistent across themes)
         - on_surface_var: '#586064' (medium gray, consistent)
         - surface_low: slightly tinted version of surface
         - primary_dim: slightly darker than primary
         - surface_high: slightly darker than surface
         - primary_container: lighter tint of primary
         - outline_var: '#abb3b7' (neutral border, consistent)
         - error: '#9f403d' (consistent across themes)
         - tertiary_container: light tint related to accent

    3. Run tests — they must all PASS (GREEN).

    4. Also add placeholder test functions (with `pass` body and `pytest.skip("Implemented in Plan 02/03")`) for TS-2 tests:
       - test_multi_font_registration
       - test_heading_body_font_separation
       - test_hangul_latin_font_split
       - test_theme_forest_colors (integration)
       - test_all_themes_generate_valid_hwpx (integration)
       - test_table_presets_from_theme (integration)
       - test_default_theme_backward_compat (integration)
  </action>
  <verify>
    <automated>cd /Users/leeeunmi/Projects/active/hwpx-skill && python -m pytest tests/test_themes.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pyhwpxlib/themes.py exists with Theme/Palette/FontSet/SizeSet/Margins frozen dataclasses
    - BUILTIN_THEMES has 10 themes, 'default' palette matches DS dict exactly
    - _make_table_presets produces correct shape with palette-derived colors
    - All non-skipped tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_themes.py -x -q` passes (non-skipped tests)
- `python -c "from pyhwpxlib.themes import BUILTIN_THEMES, Theme; print(len(BUILTIN_THEMES))"` prints 10
- `python -c "from pyhwpxlib.themes import BUILTIN_THEMES; d=BUILTIN_THEMES['default'].palette; assert d.primary=='#395da2'"` exits 0
</verification>

<success_criteria>
- Theme data model is complete and importable
- All 10 themes have fully populated palettes (no None or empty required fields)
- Default theme is byte-for-byte compatible with current DS dict values
- Test scaffold exists for entire phase (TS-1 + TS-2)
</success_criteria>

<output>
After completion, create `.planning/phases/01-theme-system-core/01-01-SUMMARY.md`
</output>
